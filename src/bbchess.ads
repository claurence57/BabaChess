--
--  AdaChess-BB - Smart Chess Engine (bitboard edition)
--
--  Learning/validation counterpart of AdaChess (mailbox). This root package
--  only gathers the BBChess hierarchy; all packages are pure (no state), the
--  mutable state being kept in explicit position records.
--

package BBChess is

   pragma Pure (BBChess);

end BBChess;
